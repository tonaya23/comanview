# Edge Migrations

SQLite migrations for the ComanView Edge (WAL mode).

Managed by Drizzle. Do NOT edit these files manually after applying them.
New migrations are generated with `drizzle-kit generate` from `packages/database`.
